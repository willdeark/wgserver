{
    "private": true,
    "scripts": {
        "docker:build": "docker build -t kuaifan/lws .",
        "docker:push": "docker push kuaifan/lws",
        "cmd:up": "./cmd up -d",
        "cmd:restart": "./cmd restart"
    }
}
